# Stage 07 transcript fixture

Bounded timestamped meeting transcript used by `just stage-exit 07`.

- `fixture-meeting.json` freezes three ordered segments with millisecond anchors.
- No audio, screenshots, calendar, or live-capture artifacts are included.
