# Audit triage ledger — 166 candidates, 2026-08-21

Base revision: `9278cdf542d6a2b1934d40e5a970b20266ed120c`

Branch: `harden/audit-166`

## Why this file exists

A 13-agent review produced 166 findings. Per the review policy this repository
works under, that output is **candidate generation, not a verdict**:

> Deterministic tests, validators, and other executable checks are the decision
> layer. LLM review is advisory candidate generation only: it cannot override a
> failed check or replace missing evidence. Every candidate finding must state a
> reachable input/trigger, current wrong output, expected output, and a concrete
> executable check. It remains advisory until that check confirms or falsifies
> it.

So each row below records what actually happened when the claim was executed.

## Dispositions

| Code | Meaning |
| --- | --- |
| `CONFIRMED` | A test reproduced it against the base, then passed after the fix. |
| `FALSIFIED` | The claim did not reproduce. Discarded, with the killing evidence. |
| `DECISION` | Real, but the remedy is a product call. Entry in `DECISIONS.md`. |
| `DEFERRED` | Real, remedy known, not in this pass. Entry in `HARDENING.md`. |

A `CONFIRMED` row is only complete when its red-proof command and output are
recorded. A fix without a test that failed first is not evidence.

## B0 — Harness

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| H-001 | P0 | No CI exists; every gate is manual | CONFIRMED | `ls .github` returned `No such file or directory`. Fixed by `.github/workflows/ci.yml`. |
| H-002 | P0 | 11 reachable CVEs, including SQL injection in `pgx` reached from `hosted` | CONFIRMED | `govulncheck ./...` reported 11 at base and **0** after bumping pgx 5.7.4 to 5.9.2, x/net 0.38 to 0.55, x/text 0.28 to 0.39, grpc 1.71 to 1.82.1, and the toolchain to go1.26.6. |
| H-003 | P0 | Generated-contract drift gate has never passed | CONFIRMED | `ruby tools/generated-manifest.rb check` exited 1 with `OURO-CONTRACT-GENERATED-DRIFT` at a clean HEAD. Drift was exactly 10 entries at +12 bytes, the width of `ouroboros` to `sentra-code-memory-v2`; the manifest was stale, not the output. Regenerated; now exit 0. |
| H-004 | P1 | `just check` covers 17 of 86 packages, omitting all broker and gateway | CONFIRMED | Package-list diff. `just check` now builds, vets, tests and gofmt-checks the whole module. |
| H-005 | P2 | No `-race` in any gate | CONFIRMED | Absent from justfile, scripts and CI. Added `just check-race` plus a CI job. Passes at base because no test exercises concurrency, so this gate stays inert until B5 lands the hammer tests. |
| H-006 | P2 | Five duplicate git-repo test helpers, one hardcoding `/usr/bin/git` | CONFIRMED | New `services/internal/testsupport`, covered by `TestRequireGitResolvesThroughPath` and `TestGitRepoIgnoresHostGitConfiguration`. |
| H-007 | P2 | Test cache never defeated; `-count=1` used nowhere | CONFIRMED | A cached PASS recorded before an edit is not evidence about the edit. All gates now pass `-count=1`. |

## B2 — Trust boundary and remote code execution

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| T-001 | P0 | `code_apply_changeset` reaches `/bin/sh -c` from a request field, ungated on the JSONL `serve` surface | CONFIRMED | Reproduced end to end against a built binary before the fix: command executed, response `ok:true`. Red test `TestHandleRefusesApplyChangeSetWithoutOperatorTrust` failed with `error_code="changeset_rejected"` (the applier was reached). Now refused with `operator_trust_required`. |
| T-002 | P0 | `hooks_local install` was equally ungated | CONFIRMED | Not in the original audit. The red run of `TestHandleRefusesHooksLocalMutationsWithoutOperatorTrust` returned `ok:true` having installed four git hooks into the fixture repository — a second execution vector, since installed hooks run on every commit. |
| T-003 | P0 | MCP opt-in is an in-band argument the model controls, advertised in `tools/list` | CONFIRMED | Trust moved to the context, granted only at process start. The field is now refused with `-32602` rather than ignored, so a client that believes it granted a permission is told otherwise. `TestMCPRejectsAnyAttemptToGrantTrustThroughArguments`. |
| T-004 | P0 | `mcp`/`http` accept a caller-chosen `root`, giving arbitrary host file read | CONFIRMED | Reproduced against the built binary: `{"root":"/","path":"etc/hosts"}` returned the file. Now `root_not_permitted`; `--root=/` remains as an explicit operator opt-out. `TestHandleRefusesARootOutsideThePin`. |
| T-005 | P1 | Verification commands run through a shell with the parent environment | CONFIRMED | Red run showed all seven shell forms executing (`;`, `&&`, `\|`, backtick, `$()`, `>`), plus `sh -c` and `bash -c` accepted and `printenv` reading a parent variable. Now an argv allowlist with a constructed environment and process-group kill. `TestVerificationRefusesShellMetacharacters`. |
| T-006 | P2 | `OperatorTrustGates` and `CatalogMetadata().RequiresOperatorTrust` could drift | FALSIFIED | They agree today. `TestOperatorTrustGatesAgreeWithCatalogMetadata` passed on its first run, so there was no defect — but nothing asserted the invariant, so the test is kept. |

## B3 — Panics and input bounds

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| C-001 | P0 | Unvalidated `top_k` panics the process on four verbs | CONFIRMED | Red run: `panic: makeslice: cap out of range` on the first case. Now bounded by `MaxTopK` with a structured refusal; `TestHandleDoesNotPanicOnHostileTopK`. |
| C-002 | P0 | `codeserve.Handle` is documented "Never panics" with no recover anywhere on the dispatch path | CONFIRMED | The claim was aspirational. A recover now backs it, writing detail to stderr and returning a stable `internal` code with no internals on the wire. `TestHandleRecoversFromAHandlerPanic`. |
| C-003 | P0 | A `.gitignore` line of `[!]` panics the indexer via `regexp.MustCompile` | CONFIRMED | Red run: `panic: regexp: Compile(...): missing closing ]`. Now compiled with `regexp.Compile` and falls back to literal matching, which is what git does. Amplified with `FuzzLoadHandlesArbitraryIgnoreFiles`: 34,384 executions, no crash. |
| C-004 | P0 | `product-brain serve` spins forever on one malformed line | CONFIRMED | Red proof against the reverted fix: **1.25 GB of output in 5 seconds** from a single bad line, one core pegged. `json.Decoder` latches a syntax error and returns it from every subsequent `Decode`. Replaced with line framing, which also lets the session resynchronise. `TestServeJSONLReturnsOnMalformedInputInsteadOfSpinning`. |
| C-005 | P0 | Crawl workers block forever on a FIFO or device symlink | CONFIRMED | Red proof: the test binary hung past its own 15s deadline and had to be killed. Only `SourceFiles` checked `IsRegular`. All walks and workers now do; the same check excludes symlinks, which also closes the out-of-root indexing path. Post-fix the same tests complete in 0.6s. |
| C-006 | P0 | Crafted WAV divides by zero in the admit path | CONFIRMED | The first version of this test passed for the wrong reason — a declared data size larger than the payload is rejected as partial before the arithmetic is reached, so the fixture never exercised it. With real data bytes: `panic: integer divide by zero`. Widened to uint64 before multiplying. |
| C-007 | P1 | `code_watch` clamps an out-of-range `timeout_ms` *up* to 24 hours | CONFIRMED | One request could hold the single-threaded serve loop for a day. Now refused above `MaxWatchTimeoutMS`. |
| C-008 | P1 | A panicking gardener worker kills the host process | CONFIRMED | Red proof with the recover removed: `panic: worker defect` escaped `RunWave`. Now converted to a failed receipt; the auto-gardener loop has its own boundary. `TestSchedulerConvertsAWorkerPanicIntoAFailedReceipt`. |
| C-009 | P2 | `Queue.Complete` error discarded, stranding a job as permanently claimed | CONFIRMED | Now recorded on the receipt rather than dropped. |

## B4 — Durable writes

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| D-001 | P0 | Local corpus compaction truncates in place, discards every write error, then deletes the delta | CONFIRMED | `os.Create` destroyed the live file before the replacement existed; every `enc.Encode` error was dropped and `Close`'s — where ENOSPC actually surfaces — was returned but only after the delta was removed. Replaced by `services/internal/durablefile`, covered by `TestWriteLeavesTheOriginalIntactWhenEmitFails`. |
| D-002 | P0 | `BurstUpsert` returns `nil` when shards fail | CONFIRMED | Red run: all 8 chunks failed with `OK:false`, `Ingested:0`, and the call still returned `err == nil`. `qualityErr` fed only a deferred telemetry span. `TestBurstUpsertReportsShardFailureInsteadOfReturningNil`. |
| D-003 | P0 | `endBatch` flush error discarded — the only disk write for a whole burst | CONFIRMED | A fully successful `IngestResult` could be returned having written nothing. Now folded into the named return. |
| D-004 | P0 | The whole memory cortex is one non-atomic `os.WriteFile` | CONFIRMED | Every claim, relation, episode, PageIndex tree and PageRank vector shares one file rewritten in place on every mutation. Now atomic via `durablefile`. |
| D-005 | P0 | HotLex compaction empties an index-only snapshot | CONFIRMED | Red run: `compaction emptied a stripped index`. Compaction replayed `addChunk`, which returns early on empty text, while `StripStoredText` clears it on every doc. A stripped index now keeps its tombstones. `TestCompactionKeepsLiveDocumentsInAStrippedIndex`. |
| D-006 | P1 | `countJSONLLines` uses the 64 KiB default scanner bound and swallows its error | CONFIRMED | Readers raise theirs to 8 MiB. One oversized line returned a short count, so compaction never fired and the delta grew without bound. |
| D-007 | P2 | Corpus, sidecar and metadata files written world-readable at 0644 | CONFIRMED | `chunks.jsonl` holds the full plaintext of every ingested document. Now 0600. |
| D-008 | P2 | Map-order iteration made every rewrite produce different bytes | CONFIRMED | Sorted, so an unchanged corpus rewrites identically and digest comparisons mean something. |

## B5 — Concurrency

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| R-001 | P0 | `memory.Store` mutators write shared state outside the lock | CONFIRMED | Red run: multiple `WARNING: DATA RACE` in `PutAgentMemoryTier` and `persistLocked`. The store's own doc comment conceded it. 21 methods touching `s.data` had no synchronisation at all. All now lock; a concurrent map write in Go is a fatal error no recover catches. |
| R-002 | P0 | Locking every method introduced 15 re-entrant deadlock chains | CONFIRMED | Found by the fix, not the audit: `sync.Mutex` is not reentrant, and the full package under `-race` hung at 240s in `ResegmentEpisode` → `BindEpisode`. Seven `*Locked` internals extracted; a detector script now shows zero remaining chains. |
| R-003 | P0 | `durableStore.flush` aliases the live chunk and sidecar maps | CONFIRMED | `d.inner.UpsertChunks` runs *before* `d.mu.Lock()`, so the inner maps are written under `inner.mu` while flush iterates them holding only `d.mu`. Proven directly by `TestFlushDoesNotAliasTheLiveChunkMap` (two `WARNING: DATA RACE` against the aliasing form). Worth noting the end-to-end ingest/flush test did **not** reproduce it — which branch flush takes decides whether the window is entered — so the deterministic pattern test is the real evidence. |
| R-004 | P2 | `-race` had no teeth | CONFIRMED | It passed from the day it was written because nothing ran two operations at once. The hammer tests are the deliverable; the gate found its first real defect immediately after they landed. |

## B6 — Determinism

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| N-001 | P1 | `resolveEdgeTarget` returns a random matching map key | CONFIRMED | Red run on the product's core retrieval path: run 1 ranked `auth03/auth.go` first, run 2 ranked `auth00/auth.go` — same corpus, same query. Now picks the shortest match, lexicographic tiebreak. `TestRankedRetrievalIsStableAcrossRuns`. |
| N-002 | P1 | Ontology PPR sorts on score with no id tiebreak | CONFIRMED | Red run against the score-only sort: `run 2 differs at 2: "leaf-03" vs "leaf-01"`. Exact ties are the normal case over a symmetric graph, not an edge case. |
| N-003 | P1 | `memory.PersonalizedPageRank` leaks dangling mass | CONFIRMED | Red run: **total mass 0.745 instead of 1.0** — 25.5% leaked — with an isolated seed deflated from 0.3 to 0.045. Only nodes that were adjacency *keys* redistributed, so a seed with no edges never propagated. Also removes an O(V²) inner loop from the answer path. `TestPersonalizedPageRankConservesMassWithDanglingSeeds`. |
| N-004 | P1 | Two phrase-node id builders disagree | CONFIRMED | One replaced spaces with underscores and cut at 48 bytes, the other used the raw phrase, so the same phrase became two disconnected nodes and seeded edges contributed nothing to the graph the query path builds. A comment claimed the prefixes matched. Unified in `PhraseNodeID`, truncating by rune. |
| N-005 | P2 | `companydoc` lexical and RRF ranking sort without a tiebreak | CONFIRMED | `tokenOverlap` ties are common, so cited documents varied per process. |
| N-006 | P2 | Passage and sibling chunk selection take a random map entry | CONFIRMED | A multi-chunk document returned a different chunk per call, so identical requests produced different evidence packs and citations. Now lowest chunk id. |
| N-007 | P2 | `hypothesisWorker` formats receipt output in map order | CONFIRMED | Its neighbour two functions above already sorted. Receipt bytes differed per run, defeating receipt digesting. |
| N-008 | P2 | `GlobalPageRank` runs a fixed 20 iterations with no convergence test | CONFIRMED | Returned an unconverged prior on a large graph and burned full passes on a small one, with no way to tell which. Now breaks on an L1 delta below tolerance. |

## B8 — Path containment and authorization

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| A-001 | P1 | Deleting `security.json` downgrades an ACL brain to `single_user` | CONFIRMED | Red run: `deleting security.json downgraded a populated brain to single_user`, where `Authorize` returns nil unconditionally. A brain that holds a corpus but has lost its ACL now refuses rather than serving. |
| A-002 | P1 | Omitting a principal is authorised as the owner | CONFIRMED | Red run: `omitting a principal was authorised as the owner (err=<nil>)`. The ACL was bypassed by *not* presenting an identity. Only the single-user profile may assume the owner now. |
| A-003 | P2 | `Authorize` discards its `action` argument | CONFIRMED | A read grant was equally an ingest and export grant, and read-only was inexpressible. `ActionGrants` added; a bare `Grants` entry still means all actions, which is what existing files mean. |
| A-004 | P1 | `ensureConfined` and `code_read` fall back to the unresolved path on `EvalSymlinks` error | CONFIRMED | Both weakened the check exactly when resolution was uncertain. Both now fail closed via the shared `services/internal/pathguard`. |
| A-005 | P2 | Containment used `HasPrefix(rel, "..")` / `Contains(rel, "..")` | CONFIRMED | Rejected legitimate names such as `..config` while catching nothing the segment-aware form misses. `TestWithinAdmitsANameBeginningWithDots`. |
| A-006 | P1 | Hooks confinement root was `<root>/.sentra`, not the repository | CONFIRMED | Asking whether `.sentra/hooks` is inside `.sentra` is trivially true even when `.sentra` symlinks elsewhere — the existing test passed only by accident, comparing an unresolved target against a resolved root. |
| A-007 | P1 | `hooks uninstall`/`status` break permanently after a repository move | CONFIRMED | Red run reproduced both exactly: `status` reported `installed=[] missing=[post-commit post-checkout post-merge pre-push]` with all four live on disk, and `uninstall` aborted with `target path escapes allowed directories` before removing anything. Paths are now recomputed from the live hooks directory. |

## B8b — Broker and gateway authorization

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| A-008 | P0 | GitHub effect broker wired to an allow-everything policy | CONFIRMED | Verified directly during the audit: `tracerAllowPolicy.Check` returned `Allowed: true` unconditionally and echoed `request.RevocationEpoch` back, making the broker's revocation comparison a tautology. It was the only policy in front of branch publication and draft-PR creation. Replaced with the real broker-backed evaluator; the type is deleted so it cannot be re-wired. `TestTracerCompositionRequiresARealPolicyBroker`. |
| A-009 | P1 | The policy adapter discards `request.Resource` | CONFIRMED | Always authorised against a fixed brain identifier, making the broker's `containsResource` check a tautology: a grant for one resource authorised every resource in the tenant. |
| A-010 | P2 | Stale-base check fails open when the base ref is absent | CONFIRMED | `if ok && baseSHA != ...` meant a deleted, renamed or misspelled base branch skipped the approved-base binding entirely and publication proceeded. Now `!ok` denies. |
| A-011 | P1 | GitHub REST URLs interpolate owner, repo and ref unescaped | CONFIRMED | Segments went into a format string with the PAT attached, so `..` escaped the `/repos/` prefix and `&`/`#` rewrote the query. All segments escaped; `per_page=100` added to the PR lookup whose correctness depended on completeness. `TestRefPathEscapeNeutralisesQueryInjection`. |

## B9 — UTF-8 safety

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| U-001 | P2 | About a dozen `truncate` helpers cut on a byte offset | CONFIRMED | Red run against the restored byte slice: `truncate("会議では…", 1) = "\xe4"`. Consolidated into `services/internal/textbound`; the correct implementation already existed as one unexported function in `hosted` and sat directly above an unsafe sibling in the same file. |
| U-002 | P1 | Meeting truncation reaches proto3 string fields | CONFIRMED | `proto.Marshal` rejects invalid UTF-8, so a non-English transcript failed at serialization rather than returning a shorter answer. |
| U-003 | P2 | Rune-documented limits compared against `len(s)` | CONFIRMED | A multibyte query was cut at roughly a third of its advertised length. `textbound.Runes` counts characters. |
| U-004 | P2 | Amplification | CONFIRMED | `FuzzBytesAlwaysValid`: 3,572,677 executions, no failure. Output is always valid UTF-8, always a prefix, always within the bound. |

## B7 — Timeouts, shutdown and resource bounds

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| L-001 | P1 | `mcp` cannot be stopped by SIGINT or SIGTERM | CONFIRMED | Verified during the audit: SIGINT left the process running and only SIGKILL ended it, so an MCP host stopping a server with SIGTERM leaked an orphan holding stdin and stdout. `signal.NotifyContext` also disables Go's default die-on-signal. Reads now run on their own goroutine and the loop selects on cancellation. `TestServeMCPReturnsWhenTheContextIsCancelledWhileIdle`. |
| L-002 | P1 | `http` prints "listening" before binding and exits 0 on bind failure | CONFIRMED | Verified against an occupied port: success banner, then `address already in use`, then exit 0 — defeating supervisor restart policies. Now binds first, announces second, exits 1 on failure. |
| L-003 | P2 | HTTP server sets only `ReadHeaderTimeout` | CONFIRMED | `MaxBytesReader` bounds a body's size but not the time taken to send it, so a trickling client held a connection and goroutine indefinitely. Read/Write/Idle timeouts and `MaxHeaderBytes` added. |
| L-004 | P1 | No file-size or file-count bound anywhere in `codecrawl` | CONFIRMED | `os.ReadFile` plus `string(raw)` held two copies of every file across up to 256 workers, with a third allocated by the symbol extractor. One large generated bundle OOM-killed the indexer, serve loop, HTTP server and watch daemon alike. `MaxIndexableFileBytes` added, with a test that it stays generous enough for real source. |

## B13 — Documentation drift

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| X-001 | P1 | `architecture.md` calls `workflow` "pure logic with no file I/O" | CONFIRMED | It stages a git worktree, runs external commands, and rewrites working-tree files. A reviewer trusting that line would not gate the verb — and did not. Corrected. |
| X-002 | P1 | Ops runbook says neither adapter provides authentication and reads obey no ACL | CONFIRMED | Both wrong: bearer-token auth, an operator-trust gate, and an ignore-policy read gate all ship. Operators following it would add redundant auth and misread the read gate as a bug. Rewritten, including the new root pin. |
| X-003 | P1 | README claims "zero-work warm reads" | CONFIRMED | The claim cannot fire in any repository with an ignore rule, and the project's own benchmark records a 4,507 ms warm refresh. Softened to "stamp-skipped warm refresh"; the underlying `stAllStampsMatch` gap is recorded in `HARDENING.md`. |
| X-004 | P2 | README "Checks" block lists 14 packages by hand | CONFIRMED | Weaker than `just check` even before the widening. Replaced with the just recipes and a pointer to CI. |
| X-005 | P2 | `watch --help` documents a `--poll` flag that was never registered | CONFIRMED | Passing it errors out. Help text now names `--fsnotify=false`. |
| X-006 | P3 | `docs/specs/performance` names an unrunnable `just modal-smoke` | CONFIRMED | No such recipe. Marked not-present-in-this-extraction, as `performance-targets.yaml` already was. |
| X-007 | P3 | `standalone.md` claims cache writes are atomic/durable | CONFIRMED | True of `code-index.gob` only when written; now true of all three stores after B4, and the sentence names the actual sequence. |

## B11 — Real implementations

| ID | Sev | Finding | Disposition | Evidence |
| --- | --- | --- | --- | --- |
| I-001 | P1 | `RunHook` is a no-op behind ~1,400 lines of installer | CONFIRMED | Red run against the restored no-op: `session log holds 0 events, want 1`. Now records the git lifecycle event, classified by hook kind, and never returns an error — a hook exiting non-zero blocks the user's commit. |
| I-002 | P1 | `sessionlog.Append` has no non-test caller, so session memory is always empty | CONFIRMED | The same wiring closes it: git lifecycle events are the durable local facts the log was built to hold. `session_continuation` and `session_recall` now have a producer. |
| I-003 | P2 | DOCS gate checks for the characters `//` | CONFIRMED | A change set reached `FACTORY_GATE_STATUS_PASSED` with "documentation" proven by a stray `// TODO`. Now enforces doc comments on every exported declaration. `TestExportedDeclarationsAreDocumented`. |
| I-004 | P1 | BUILD and TEST gates do not build or test | DEFERRED | Not fixable inside the current signature: `evaluateFactoryGate` receives in-memory post-image bytes with no repository root and no execution sandbox, and a package does not compile in isolation from its module. Compiling a fragment and calling it a build would be the same overclaiming in a new form. The `go build -overlay` design, and what it needs threaded through, is recorded in `HARDENING.md`. The gate's doc comment now states plainly what each gate proves. |
| I-005 | P1 | `savings` presents bytes÷4 estimates as measurements, over a strawman baseline, with no producer | DEFERRED | A real tokenizer is a dependency decision and an honest baseline is a design choice, not a correction. Recorded in `HARDENING.md` with the specific remedy. |

## B1, B10, B12

Populated during triage. Each row carries the red-proof command and its output.
