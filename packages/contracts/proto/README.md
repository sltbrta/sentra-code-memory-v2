# Contract Proto source

This directory is the sole hand-authored contract authority. Files use the
stable `ouroboros.contracts.v1` package and are checked by Buf STANDARD lint and
FILE compatibility rules. Generated language and JSON Schema projections live
outside this directory under `../gen/`; do not hand-edit them.
