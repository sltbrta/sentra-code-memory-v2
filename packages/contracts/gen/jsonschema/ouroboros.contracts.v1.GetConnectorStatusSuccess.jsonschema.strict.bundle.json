{
  "$defs": {
    "ouroboros.contracts.v1.GetConnectorStatusSuccess.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "GetConnectorStatusSuccess discloses non-sensitive readiness for one connection.",
      "properties": {
        "aclEpoch": {
          "description": "acl_epoch is the current ACL epoch for the connection.",
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        "connectionId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "connection_id echoes the opaque connection identity."
        },
        "cursor": {
          "description": "cursor is the last trusted complete cursor.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        },
        "issueObjectCount": {
          "description": "issue_object_count is the admitted issue evidence object count.",
          "maximum": 4294967295,
          "minimum": 0,
          "type": "integer"
        },
        "lastErrorCode": {
          "description": "last_error_code is empty when healthy; otherwise a stable non-sensitive code.",
          "maxLength": 64,
          "pattern": "^(|rate_limited|partial_snapshot|provider_unavailable|malformed_page)$",
          "type": "string"
        },
        "provider": {
          "description": "provider is always github for this Stage 08 surface.",
          "enum": [
            "github"
          ],
          "minLength": 1,
          "type": "string"
        },
        "repositoryObjectCount": {
          "description": "repository_object_count is the admitted repository evidence object count.",
          "maximum": 4294967295,
          "minimum": 0,
          "type": "integer"
        },
        "sourceScope": {
          "description": "source_scope is the exact authorized scope.",
          "maxLength": 196,
          "minLength": 1,
          "type": "string"
        },
        "state": {
          "description": "state is READY or DEGRADED for a queryable connection.",
          "enum": [
            "CONNECTOR_LIFECYCLE_STATE_READY",
            "CONNECTOR_LIFECYCLE_STATE_DEGRADED"
          ],
          "title": "Connector Lifecycle State",
          "type": "string"
        }
      },
      "required": [
        "connectionId",
        "state",
        "sourceScope",
        "provider",
        "cursor",
        "repositoryObjectCount",
        "issueObjectCount",
        "lastErrorCode",
        "aclEpoch"
      ],
      "title": "Get Connector Status Success",
      "type": "object"
    },
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.GetConnectorStatusSuccess.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.GetConnectorStatusSuccess.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
