{
  "$defs": {
    "ouroboros.contracts.v1.AdmitChangeIntentSuccess.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "AdmitChangeIntentSuccess returns the opened run identity and its state.",
      "properties": {
        "runId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "run_id is the server-authored admitted run identity."
        },
        "state": {
          "description": "state is the bounded initial run lifecycle state.",
          "enum": [
            "CHANGE_RUN_STATE_PLANNING",
            "CHANGE_RUN_STATE_READY",
            "CHANGE_RUN_STATE_RUNNING",
            "CHANGE_RUN_STATE_REVIEW",
            "CHANGE_RUN_STATE_CANDIDATE_READY",
            "CHANGE_RUN_STATE_COMPLETED",
            "CHANGE_RUN_STATE_FAILED",
            "CHANGE_RUN_STATE_CANCELLED"
          ],
          "title": "Change Run State",
          "type": "string"
        }
      },
      "required": [
        "runId",
        "state"
      ],
      "title": "Admit Change Intent Success",
      "type": "object"
    },
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.AdmitChangeIntentSuccess.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.AdmitChangeIntentSuccess.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
