{
  "$defs": {
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    },
    "ouroboros.contracts.v1.RevokeMeetingSuccess.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "RevokeMeetingSuccess confirms the meeting is revoked.",
      "properties": {
        "meetingSessionId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "meeting_session_id is the opaque revoked meeting identity."
        },
        "state": {
          "description": "state is always REVOKED on success.",
          "enum": [
            "MEETING_LIFECYCLE_STATE_REVOKED"
          ],
          "title": "Meeting Lifecycle State",
          "type": "string"
        }
      },
      "required": [
        "meetingSessionId",
        "state"
      ],
      "title": "Revoke Meeting Success",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.RevokeMeetingSuccess.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.RevokeMeetingSuccess.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
