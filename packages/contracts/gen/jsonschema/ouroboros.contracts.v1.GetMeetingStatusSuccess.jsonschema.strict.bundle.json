{
  "$defs": {
    "ouroboros.contracts.v1.GetMeetingStatusSuccess.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "GetMeetingStatusSuccess discloses non-sensitive readiness for one meeting.",
      "properties": {
        "meetingSessionId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "meeting_session_id echoes the opaque meeting identity."
        },
        "notifyReminderRecorded": {
          "description": "notify_reminder_recorded confirms the participant-notification reminder.",
          "type": "boolean"
        },
        "retention": {
          "$ref": "#/$defs/ouroboros.contracts.v1.ImportRetentionPolicy.jsonschema.strict.json",
          "description": "retention echoes the selected retention policy."
        },
        "segmentCount": {
          "description": "segment_count is the admitted segment count.",
          "exclusiveMaximum": 4294967296,
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        "sourceScope": {
          "description": "source_scope is the explicit authorized import scope.",
          "maxLength": 128,
          "minLength": 1,
          "type": "string"
        },
        "state": {
          "description": "state is a present defined lifecycle state other than PURGED for status reads.",
          "enum": [
            "MEETING_LIFECYCLE_STATE_READY",
            "MEETING_LIFECYCLE_STATE_PARTIAL"
          ],
          "title": "Meeting Lifecycle State",
          "type": "string"
        },
        "timelineEndMillis": {
          "description": "timeline_end_millis is the latest segment end.",
          "exclusiveMinimum": 0,
          "type": "integer"
        },
        "timelineStartMillis": {
          "description": "timeline_start_millis is the earliest segment start.",
          "minimum": 0,
          "type": "integer"
        },
        "title": {
          "description": "title is the admitted meeting title metadata.",
          "maxLength": 256,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "meetingSessionId",
        "state",
        "segmentCount",
        "timelineStartMillis",
        "timelineEndMillis",
        "retention",
        "notifyReminderRecorded",
        "sourceScope",
        "title"
      ],
      "title": "Get Meeting Status Success",
      "type": "object"
    },
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    },
    "ouroboros.contracts.v1.ImportRetentionPolicy.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "ImportRetentionPolicy is the Stage 07 required retention selection. Every\n class must be chosen before import; missing retention fails closed at the\n boundary and never defaults raw media silently.",
      "properties": {
        "derivativeRetention": {
          "description": "derivative_retention is the selected transcript/notes retention policy.",
          "maxLength": 32,
          "minLength": 1,
          "type": "string"
        },
        "rawMediaRetention": {
          "description": "raw_media_retention is the selected raw-media retention policy.",
          "maxLength": 32,
          "minLength": 1,
          "type": "string"
        },
        "screenshotRetention": {
          "description": "screenshot_retention is the selected screenshot retention policy.",
          "maxLength": 32,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "rawMediaRetention",
        "screenshotRetention",
        "derivativeRetention"
      ],
      "title": "Import Retention Policy",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.GetMeetingStatusSuccess.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.GetMeetingStatusSuccess.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
