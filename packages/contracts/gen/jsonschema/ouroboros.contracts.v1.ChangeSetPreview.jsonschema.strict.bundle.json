{
  "$defs": {
    "google.protobuf.Timestamp.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "format": "date-time",
      "title": "Timestamp",
      "type": "string"
    },
    "ouroboros.contracts.v1.ArtifactRef.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "ArtifactRef identifies an immutable artifact without granting read access.",
      "properties": {
        "artifactId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "artifact_id is an opaque artifact identity."
        },
        "contentDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "content_digest binds the reference to immutable content."
        },
        "tenantId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "tenant_id scopes the artifact identity and prevents cross-tenant reuse."
        }
      },
      "required": [
        "artifactId",
        "contentDigest",
        "tenantId"
      ],
      "title": "Artifact Ref",
      "type": "object"
    },
    "ouroboros.contracts.v1.CausalContext.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "CausalContext binds messages to an ordered, fenced causal history.",
      "properties": {
        "causationId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "causation_id names the immediate triggering event or command."
        },
        "correlationId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "correlation_id groups one user-visible operation."
        },
        "fence": {
          "description": "fence prevents an expired lease holder from committing.",
          "minimum": 0,
          "type": "integer"
        },
        "sequence": {
          "description": "sequence is monotonically assigned by the canonical ledger.",
          "minimum": 0,
          "type": "integer"
        },
        "traceId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "trace_id identifies a separately authorized trace stream."
        },
        "watermark": {
          "description": "watermark records the complete source position observed by a projection.",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "correlationId",
        "causationId",
        "traceId",
        "sequence",
        "fence",
        "watermark"
      ],
      "title": "Causal Context",
      "type": "object"
    },
    "ouroboros.contracts.v1.ChangeSet.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "ChangeSet is an atomic candidate edit set tied to one exact Git base.",
      "properties": {
        "baseGitOid": {
          "description": "base_git_oid is the exact Git tree or commit expected at application time.",
          "maxLength": 64,
          "minLength": 1,
          "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$",
          "type": "string"
        },
        "changeSetDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "change_set_digest binds candidate edits, base, and metadata."
        },
        "changeSetId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "change_set_id is an opaque candidate identity."
        },
        "patchArtifact": {
          "$ref": "#/$defs/ouroboros.contracts.v1.ArtifactRef.jsonschema.strict.json",
          "description": "patch_artifact contains the immutable multi-file candidate patch."
        },
        "rollbackArtifact": {
          "$ref": "#/$defs/ouroboros.contracts.v1.ArtifactRef.jsonschema.strict.json",
          "description": "rollback_artifact identifies the deterministic candidate rollback representation."
        },
        "validationReceipts": {
          "description": "validation_receipts record deterministic gates run on the isolated candidate.",
          "items": {
            "$ref": "#/$defs/ouroboros.contracts.v1.Receipt.jsonschema.strict.json"
          },
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "changeSetId",
        "baseGitOid",
        "patchArtifact",
        "changeSetDigest",
        "rollbackArtifact"
      ],
      "title": "Change Set",
      "type": "object"
    },
    "ouroboros.contracts.v1.ChangeSetPreview.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "ChangeSetPreview is the served atomic multi-file candidate preview: one\n exact-base ChangeSet bound to the admitted run's approved Git base, its\n normalized per-file edits, per-language obligations, the gate roster, and\n rollback facts. Application is all-or-nothing; a rejected candidate carries\n a rollback receipt and canonical source is never mutated by this boundary.",
      "properties": {
        "candidateState": {
          "description": "candidate_state is the bounded all-or-nothing lifecycle state.",
          "enum": [
            "CANDIDATE_STATE_PROPOSED",
            "CANDIDATE_STATE_APPLIED",
            "CANDIDATE_STATE_VERIFIED",
            "CANDIDATE_STATE_REVIEWED",
            "CANDIDATE_STATE_RETAINED",
            "CANDIDATE_STATE_REJECTED"
          ],
          "title": "Candidate State",
          "type": "string"
        },
        "changeSet": {
          "$ref": "#/$defs/ouroboros.contracts.v1.ChangeSet.jsonschema.strict.json",
          "description": "change_set is the immutable exact-base atomic candidate."
        },
        "edits": {
          "description": "edits is the normalized per-file candidate edit set.",
          "items": {
            "$ref": "#/$defs/ouroboros.contracts.v1.PreviewEdit.jsonschema.strict.json"
          },
          "maxItems": 64,
          "minItems": 1,
          "type": "array"
        },
        "expectedBaseGitOid": {
          "description": "expected_base_git_oid echoes the admitted run's approved intent base; the\n candidate base must equal it, so no preview can describe another commit.",
          "maxLength": 64,
          "minLength": 1,
          "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$",
          "type": "string"
        },
        "gates": {
          "description": "gates is the bounded roster evaluated on the isolated candidate.",
          "items": {
            "$ref": "#/$defs/ouroboros.contracts.v1.GateSpec.jsonschema.strict.json"
          },
          "maxItems": 8,
          "minItems": 4,
          "type": "array"
        },
        "obligations": {
          "description": "obligations carries exactly one obligation per touched P5 language.",
          "items": {
            "$ref": "#/$defs/ouroboros.contracts.v1.LanguageObligation.jsonschema.strict.json"
          },
          "maxItems": 5,
          "minItems": 1,
          "type": "array"
        },
        "rollbackReceipt": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Receipt.jsonschema.strict.json",
          "description": "rollback_receipt records the deterministic discard of a rejected candidate."
        }
      },
      "required": [
        "changeSet",
        "candidateState",
        "expectedBaseGitOid"
      ],
      "title": "Change Set Preview",
      "type": "object"
    },
    "ouroboros.contracts.v1.Digest.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Digest identifies immutable bytes using a named digest algorithm.",
      "properties": {
        "algorithm": {
          "description": "algorithm identifies the canonical digest algorithm, for example sha256.",
          "maxLength": 32,
          "minLength": 1,
          "type": "string"
        },
        "hex": {
          "description": "hex is the lower-case encoded digest value.",
          "maxLength": 256,
          "minLength": 1,
          "pattern": "^[0-9a-f]+$",
          "type": "string"
        }
      },
      "required": [
        "algorithm",
        "hex"
      ],
      "title": "Digest",
      "type": "object"
    },
    "ouroboros.contracts.v1.EvidenceRef.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "EvidenceRef identifies evidence metadata and optionally one precise anchor.",
      "properties": {
        "anchorDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "anchor_digest identifies an optional canonical anchor serialization."
        },
        "evidenceId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "evidence_id is an opaque evidence identity."
        },
        "sourceRevisionId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "source_revision_id identifies the immutable source revision."
        }
      },
      "required": [
        "evidenceId",
        "sourceRevisionId"
      ],
      "title": "Evidence Ref",
      "type": "object"
    },
    "ouroboros.contracts.v1.GateSpec.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "GateSpec binds one required or declared deterministic gate to its result.",
      "properties": {
        "gateId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "gate_id is an opaque gate evaluation identity."
        },
        "kind": {
          "description": "kind is the bounded gate vocabulary kind.",
          "enum": [
            "FACTORY_GATE_KIND_BUILD",
            "FACTORY_GATE_KIND_TEST",
            "FACTORY_GATE_KIND_DOCS",
            "FACTORY_GATE_KIND_SECURITY"
          ],
          "title": "Factory Gate Kind",
          "type": "string"
        },
        "required": {
          "description": "required marks the non-removable build, test, docs, and security gates.",
          "type": "boolean"
        },
        "status": {
          "description": "status is the bounded evaluation state on the isolated candidate.",
          "enum": [
            "FACTORY_GATE_STATUS_PENDING",
            "FACTORY_GATE_STATUS_RUNNING",
            "FACTORY_GATE_STATUS_PASSED",
            "FACTORY_GATE_STATUS_FAILED"
          ],
          "title": "Factory Gate Status",
          "type": "string"
        }
      },
      "required": [
        "gateId",
        "kind",
        "required",
        "status"
      ],
      "title": "Gate Spec",
      "type": "object"
    },
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    },
    "ouroboros.contracts.v1.ImpactReceipt.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "ImpactReceipt records graph-derived obligations before source mutation is offered.",
      "properties": {
        "affectedArtifacts": {
          "description": "affected_artifacts identify code, docs, config, and deployment obligations.",
          "items": {
            "$ref": "#/$defs/ouroboros.contracts.v1.ArtifactRef.jsonschema.strict.json"
          },
          "type": "array"
        },
        "baseGitOid": {
          "description": "base_git_oid is the exact source snapshot analyzed.",
          "type": "string"
        },
        "receipt": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Receipt.jsonschema.strict.json",
          "description": "receipt is the canonical impact-analysis result."
        },
        "uncertaintyReasons": {
          "description": "uncertainty_reasons disclose incomplete graph or index coverage.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "baseGitOid"
      ],
      "title": "Impact Receipt",
      "type": "object"
    },
    "ouroboros.contracts.v1.LanguageObligation.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "LanguageObligation freezes the impact, documentation, and test obligations\n owed for one touched P5 language before a candidate may verify.",
      "properties": {
        "docsRequired": {
          "description": "docs_required records the documentation obligation for this lane.",
          "type": "boolean"
        },
        "impact": {
          "$ref": "#/$defs/ouroboros.contracts.v1.ImpactReceipt.jsonschema.strict.json",
          "description": "impact is the graph-derived impact analysis for this lane."
        },
        "language": {
          "description": "language identifies the touched P5 language lane.",
          "enum": [
            "CODE_LANGUAGE_GO",
            "CODE_LANGUAGE_TYPESCRIPT",
            "CODE_LANGUAGE_PYTHON",
            "CODE_LANGUAGE_RUST",
            "CODE_LANGUAGE_JAVA"
          ],
          "title": "Code Language",
          "type": "string"
        },
        "testsRequired": {
          "description": "tests_required records the test obligation for this lane.",
          "type": "boolean"
        }
      },
      "required": [
        "language",
        "impact",
        "docsRequired",
        "testsRequired"
      ],
      "title": "Language Obligation",
      "type": "object"
    },
    "ouroboros.contracts.v1.PreviewEdit.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "PreviewEdit is one normalized per-file candidate edit tied to one P5 lane.",
      "properties": {
        "afterDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "after_digest pins the exact post-image file bytes when they exist."
        },
        "beforeDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "before_digest pins the exact pre-image file bytes when they exist."
        },
        "language": {
          "description": "language binds the edit to exactly one P5 language lane.",
          "enum": [
            "CODE_LANGUAGE_GO",
            "CODE_LANGUAGE_TYPESCRIPT",
            "CODE_LANGUAGE_PYTHON",
            "CODE_LANGUAGE_RUST",
            "CODE_LANGUAGE_JAVA"
          ],
          "title": "Code Language",
          "type": "string"
        },
        "oldPath": {
          "description": "old_path is the normalized pre-image path for rename edits only.",
          "maxLength": 4096,
          "type": "string"
        },
        "operation": {
          "description": "operation is the bounded per-file edit vocabulary.",
          "enum": [
            "PREVIEW_EDIT_OPERATION_ADD",
            "PREVIEW_EDIT_OPERATION_MODIFY",
            "PREVIEW_EDIT_OPERATION_DELETE",
            "PREVIEW_EDIT_OPERATION_RENAME"
          ],
          "title": "Preview Edit Operation",
          "type": "string"
        },
        "path": {
          "description": "path is the normalized post-image repository-relative path.",
          "maxLength": 4096,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "path",
        "operation",
        "language"
      ],
      "title": "Preview Edit",
      "type": "object"
    },
    "ouroboros.contracts.v1.Receipt.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Receipt records the authoritative outcome of an admitted operation.",
      "properties": {
        "causal": {
          "$ref": "#/$defs/ouroboros.contracts.v1.CausalContext.jsonschema.strict.json",
          "description": "causal binds the outcome to replayable ordering and fencing state."
        },
        "configurationDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "configuration_digest pins the effective configuration without secret plaintext."
        },
        "evidence": {
          "description": "evidence records immutable inputs and outputs supporting this outcome.",
          "items": {
            "$ref": "#/$defs/ouroboros.contracts.v1.EvidenceRef.jsonschema.strict.json"
          },
          "type": "array"
        },
        "operationId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "operation_id identifies the command, run, or effect being receipted."
        },
        "reasonCode": {
          "description": "reason_code is a non-sensitive stable reason, never an upstream error body.",
          "type": "string"
        },
        "receiptId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "receipt_id is a globally unique opaque receipt identifier."
        },
        "recordedAt": {
          "$ref": "#/$defs/google.protobuf.Timestamp.jsonschema.strict.json",
          "description": "recorded_at is when the receipt became canonical."
        },
        "status": {
          "description": "status is a stable, machine-readable outcome.",
          "enum": [
            "RECEIPT_STATUS_ACCEPTED",
            "RECEIPT_STATUS_REJECTED",
            "RECEIPT_STATUS_DEFERRED",
            "RECEIPT_STATUS_COMPLETED",
            "RECEIPT_STATUS_PARTIAL"
          ],
          "title": "Receipt Status",
          "type": "string"
        }
      },
      "required": [
        "receiptId",
        "status",
        "reasonCode",
        "operationId",
        "causal",
        "recordedAt",
        "configurationDigest"
      ],
      "title": "Receipt",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.ChangeSetPreview.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.ChangeSetPreview.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
