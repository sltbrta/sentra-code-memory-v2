{
  "$defs": {
    "ouroboros.contracts.v1.Cursor.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Cursor pins an ordered replay or status position without exposing storage internals.",
      "properties": {
        "token": {
          "description": "token is an opaque bounded continuation token.",
          "maxLength": 512,
          "type": "string"
        },
        "watermark": {
          "description": "watermark is the inclusive canonical event watermark observed by the caller.",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "watermark",
        "token"
      ],
      "title": "Cursor",
      "type": "object"
    },
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    },
    "ouroboros.contracts.v1.ReadStatusRequest.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "ReadStatusRequest requests bounded status for the authenticated local session.",
      "properties": {
        "after": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Cursor.jsonschema.strict.json",
          "description": "after limits status/event summaries to a bounded ordered cursor."
        },
        "requestedSession": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "requested_session is untrusted and must match the authenticated session context."
        }
      },
      "required": [
        "requestedSession"
      ],
      "title": "Read Status Request",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.ReadStatusRequest.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.ReadStatusRequest.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
