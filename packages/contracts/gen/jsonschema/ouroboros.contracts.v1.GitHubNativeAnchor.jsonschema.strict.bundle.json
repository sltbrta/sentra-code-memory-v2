{
  "$defs": {
    "ouroboros.contracts.v1.Digest.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Digest identifies immutable bytes using a named digest algorithm.",
      "properties": {
        "algorithm": {
          "description": "algorithm identifies the canonical digest algorithm, for example sha256.",
          "maxLength": 32,
          "minLength": 1,
          "type": "string"
        },
        "hex": {
          "description": "hex is the lower-case encoded digest value.",
          "maxLength": 256,
          "minLength": 1,
          "pattern": "^[0-9a-f]+$",
          "type": "string"
        }
      },
      "required": [
        "algorithm",
        "hex"
      ],
      "title": "Digest",
      "type": "object"
    },
    "ouroboros.contracts.v1.EvidenceRef.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "EvidenceRef identifies evidence metadata and optionally one precise anchor.",
      "properties": {
        "anchorDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "anchor_digest identifies an optional canonical anchor serialization."
        },
        "evidenceId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "evidence_id is an opaque evidence identity."
        },
        "sourceRevisionId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "source_revision_id identifies the immutable source revision."
        }
      },
      "required": [
        "evidenceId",
        "sourceRevisionId"
      ],
      "title": "Evidence Ref",
      "type": "object"
    },
    "ouroboros.contracts.v1.GitHubNativeAnchor.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "GitHubNativeAnchor locates one provider-native repository or issue span.",
      "properties": {
        "endLine": {
          "description": "end_line is exclusive for file anchors; zero when unused.",
          "maximum": 4294967295,
          "minimum": 0,
          "type": "integer"
        },
        "evidence": {
          "$ref": "#/$defs/ouroboros.contracts.v1.EvidenceRef.jsonschema.strict.json",
          "description": "evidence identifies the cited immutable evidence and source revision."
        },
        "issueNumber": {
          "description": "issue_number is the provider issue number for issue anchors.",
          "maximum": 4294967295,
          "minimum": 0,
          "type": "integer"
        },
        "kind": {
          "description": "kind is repository_file or issue.",
          "maxLength": 32,
          "minLength": 1,
          "type": "string"
        },
        "owner": {
          "description": "owner is the GitHub repository owner.",
          "maxLength": 64,
          "minLength": 1,
          "pattern": "^[A-Za-z0-9](?:[A-Za-z0-9-]{0,38}[A-Za-z0-9])?$",
          "type": "string"
        },
        "path": {
          "description": "path is the repository-relative file path for repository_file anchors.",
          "maxLength": 512,
          "type": "string"
        },
        "repo": {
          "description": "repo is the GitHub repository name.",
          "maxLength": 100,
          "minLength": 1,
          "pattern": "^[A-Za-z0-9._-]{1,100}$",
          "type": "string"
        },
        "startLine": {
          "description": "start_line is inclusive for file anchors; zero when unused.",
          "maximum": 4294967295,
          "minimum": 0,
          "type": "integer"
        },
        "supportingTextDigest": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Digest.jsonschema.strict.json",
          "description": "supporting_text_digest binds hydrated supporting bytes without prose."
        }
      },
      "required": [
        "kind",
        "owner",
        "repo",
        "path",
        "issueNumber",
        "startLine",
        "endLine",
        "supportingTextDigest",
        "evidence"
      ],
      "title": "Git Hub Native Anchor",
      "type": "object"
    },
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.GitHubNativeAnchor.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.GitHubNativeAnchor.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
