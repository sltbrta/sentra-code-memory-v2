{
  "$defs": {
    "ouroboros.contracts.v1.AuthenticatedPrincipalRef.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "AuthenticatedPrincipalRef is an identity assertion emitted by a trusted boundary.",
      "properties": {
        "principalId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "principal_id is never accepted as caller identity from an untrusted body."
        },
        "sessionId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "session_id identifies the authenticated session when a session exists."
        },
        "tenantId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "tenant_id is the authenticated tenant context."
        }
      },
      "required": [
        "principalId",
        "tenantId"
      ],
      "title": "Authenticated Principal Ref",
      "type": "object"
    },
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    },
    "ouroboros.contracts.v1.ReconcileSourceRequest.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "ReconcileSourceRequest advances only between exact local committed revisions.",
      "properties": {
        "caller": {
          "$ref": "#/$defs/ouroboros.contracts.v1.UntrustedIngestionCaller.jsonschema.strict.json",
          "description": "caller is untrusted payload identity to cross-check against authenticated context."
        },
        "expectedCommitOid": {
          "description": "expected_commit_oid prevents reconciliation from a stale committed revision.",
          "minLength": 1,
          "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$",
          "type": "string"
        },
        "expectedGenerationId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "expected_generation_id prevents replacement of a newer generation."
        },
        "idempotencyKey": {
          "description": "idempotency_key distinguishes exact retries from conflicts.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        },
        "sourceId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "source_id is resolved under authenticated tenant and brain scope."
        },
        "targetCommitOid": {
          "description": "target_commit_oid is resolved locally to a server-authored snapshot.",
          "minLength": 1,
          "pattern": "^[0-9a-f]{40}([0-9a-f]{24})?$",
          "type": "string"
        }
      },
      "required": [
        "caller",
        "sourceId",
        "expectedGenerationId",
        "expectedCommitOid",
        "targetCommitOid",
        "idempotencyKey"
      ],
      "title": "Reconcile Source Request",
      "type": "object"
    },
    "ouroboros.contracts.v1.UntrustedIngestionCaller.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "UntrustedIngestionCaller carries body identity only for authenticated-context checks.",
      "properties": {
        "requestedPrincipal": {
          "$ref": "#/$defs/ouroboros.contracts.v1.AuthenticatedPrincipalRef.jsonschema.strict.json",
          "description": "requested_principal must match the authenticated peer principal and tenant."
        },
        "requestedSession": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "requested_session must match the authenticated gateway session."
        }
      },
      "required": [
        "requestedPrincipal",
        "requestedSession"
      ],
      "title": "Untrusted Ingestion Caller",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.ReconcileSourceRequest.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.ReconcileSourceRequest.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
