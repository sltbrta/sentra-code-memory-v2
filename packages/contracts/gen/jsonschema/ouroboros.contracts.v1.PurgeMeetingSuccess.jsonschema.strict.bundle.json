{
  "$defs": {
    "ouroboros.contracts.v1.Identifier.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "Identifier is an opaque, stable identifier within a declared namespace.",
      "properties": {
        "namespace": {
          "description": "namespace prevents an identifier string from crossing a domain boundary.",
          "maxLength": 64,
          "minLength": 1,
          "type": "string"
        },
        "value": {
          "description": "value is opaque to consumers and must be non-empty after boundary validation.",
          "maxLength": 512,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "namespace",
        "value"
      ],
      "title": "Identifier",
      "type": "object"
    },
    "ouroboros.contracts.v1.PurgeMeetingSuccess.jsonschema.strict.json": {
      "$schema": "https://json-schema.org/draft/2020-12/schema",
      "additionalProperties": false,
      "description": "PurgeMeetingSuccess confirms purge completion.",
      "properties": {
        "meetingSessionId": {
          "$ref": "#/$defs/ouroboros.contracts.v1.Identifier.jsonschema.strict.json",
          "description": "meeting_session_id is the opaque purged meeting identity."
        },
        "purgedArtifactCount": {
          "description": "purged_artifact_count is the number of encrypted transcript artifacts purged.",
          "maximum": 4294967295,
          "minimum": 0,
          "type": "integer"
        },
        "state": {
          "description": "state is always PURGED on success.",
          "enum": [
            "MEETING_LIFECYCLE_STATE_PURGED"
          ],
          "title": "Meeting Lifecycle State",
          "type": "string"
        }
      },
      "required": [
        "meetingSessionId",
        "state",
        "purgedArtifactCount"
      ],
      "title": "Purge Meeting Success",
      "type": "object"
    }
  },
  "$id": "ouroboros.contracts.v1.PurgeMeetingSuccess.jsonschema.strict.bundle.json",
  "$ref": "#/$defs/ouroboros.contracts.v1.PurgeMeetingSuccess.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
