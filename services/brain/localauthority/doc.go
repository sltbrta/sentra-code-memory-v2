// Package localauthority is the product-owned local authority composition root
// (ADR 0022). It binds durable SQLite, encrypted artifact vault, committed-Git
// ingestion, codeindex, grounded query, and conversation vault for the
// product authority process (ouroboros-local-authority).
//
// Company-doc residual ask and continual docs live on product-brain / hosted.
// This package is substrate for peer-auth RPC surfaces — not a second product.
//
// Frozen snapshot: archive/2026-07-stage-retirement/services/brain/localauthority
package localauthority
