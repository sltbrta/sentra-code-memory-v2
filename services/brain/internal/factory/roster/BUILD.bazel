"""Durable Stage 05 leaf lease and leaf-result ledger facts."""

load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "roster",
    srcs = [
        "doc.go",
        "identity.go",
        "roster.go",
    ],
    importpath = "github.com/sltbrta/sentra-code-memory-v2/services/brain/internal/factory/roster",
    visibility = ["//services/brain:__subpackages__"],
    deps = [
        "//services/internal/contracts",
    ],
)

go_test(
    name = "roster_test",
    srcs = glob(["*_test.go"]),
    embed = [":roster"],
    tags = [
        "ouroboros_affected",
        "ouroboros_check",
        "ouroboros_test",
    ],
    deps = [
        "//services/brain/internal/localstate",
        "//services/brain/internal/localstate/schema",
        "//services/internal/contracts",
        "@org_modernc_sqlite//:go_default_library",
    ],
)
