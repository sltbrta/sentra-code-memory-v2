"""Stage 06 Tracer 001 typed one-layer DAG compiler."""

load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "tracer001",
    srcs = [
        "compile.go",
        "doc.go",
        "errors.go",
        "paths.go",
        "types.go",
    ],
    importpath = "github.com/sltbrta/sentra-code-memory-v2/services/brain/internal/factory/tracer001",
    visibility = [
        "//services/brain:__subpackages__",
        "//services/broker:__subpackages__",
        "//services/gateway:__subpackages__",
    ],
    deps = ["//services/internal/contracts"],
)

go_test(
    name = "tracer001_test",
    srcs = ["compile_test.go"],
    embed = [":tracer001"],
    tags = [
        "ouroboros_affected",
        "ouroboros_check",
        "ouroboros_test",
    ],
    visibility = ["//tools/stage-06:__pkg__"],
)
