"""Immutable scoped evidence metadata ledger."""

load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "evidenceledger",
    srcs = ["ledger.go"],
    importpath = "github.com/sltbrta/sentra-code-memory-v2/services/brain/internal/evidenceledger",
    visibility = ["//services/brain:__subpackages__"],
    deps = ["//services/internal/contracts"],
)

go_test(
    name = "evidenceledger_test",
    srcs = ["ledger_test.go"],
    embed = [":evidenceledger"],
    deps = ["//services/internal/contracts"],
    tags = [
        "ouroboros_affected",
        "ouroboros_check",
        "ouroboros_test",
    ],
    visibility = ["//tools/stage-02:__pkg__"],
)
